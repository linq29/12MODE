## Legacy Static Pages

This directory stores the pre-Vite static implementation (`.html` + `css/` + `js/`).

Current active frontend is the React + Vite app in `client/`.
Keep these files for reference only.
